language: node_js
node_js:
  - "node"
#sudo: true
dist: trusty

branches:
  only:
    - master

before_script:
  - npm install -g @angular/cli

#script:
#  - npm install
#  - ng build --prod

after_success:
  - ./transfer_to_ftp_dummy.sh
#  - ./transfer_to_ftp.sh
